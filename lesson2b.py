#2. DATA TYPES:Used to define the type of data in a variable
#Because it specifies the operations to be done
#Categorised into two
#1. Primary Data Types (Strings(text), Integers(whole numbers),
#Floating Points(fractions), Boolean(True/ False))
#2. Secondary: Collections -> Lists[], Tuple(), Dictionary{}, Sets{}

#Strings: Textual Data
#Enclosed inside double/ single
#sequence of Characters

string1 = "I love" + " "
string2 = "Programming"

#1. Concatenation(merge/ join strings) -> +
whole_string = string1 + string2
print(whole_string)

#2. Length
#len(): Returns the number of characters in a string
language = "Kotlin"
print(len(language))

#type(): Determine datatype
print(type(language))
